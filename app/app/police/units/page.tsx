import type { Metadata } from "next";
import OpsUnits from "../../../components/OpsUnits";

export const metadata: Metadata = { title: "Units | Dubai Police" };

export default function UnitsPage() {
  return <OpsUnits />;
}
